
import { 
  Clock as ClockIcon, 
  Users as UsersIcon,
  Tag as BadgeIcon 
} from "lucide-react";

// Re-export the icons with the names used in the application
export const Clock = ClockIcon;
export const Users = UsersIcon;
export const Badge = BadgeIcon;
